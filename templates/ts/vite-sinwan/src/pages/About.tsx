import { cc } from "sinwan/component";

export const About = cc(() => (
  <section id="center">
    <div>
      <h1>About</h1>
      <p>
        This starter uses <code>sinwan-router</code> for client-side routing.
      </p>
    </div>
  </section>
));
