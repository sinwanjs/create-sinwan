{
  "name": "{{PROJECT_NAME}}",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "sinwan": "{{SINWAN_VERSION}}"
  },
  "devDependencies": {
    "vite": "{{VITE_VERSION}}",
    "vite-plugin-sinwan": "{{VITE_PLUGIN_SINWAN_VERSION}}",
    "tailwindcss": "{{TAILWINDCSS_VERSION}}",
    "@tailwindcss/vite": "{{TAILWINDCSS_VITE_VERSION}}"
  }
}
